
.env
.env.local
.env.*
!.env.example

# Claude Code worktrees
.claude/worktrees/

# Sidecar tool data
.sidecar/
.todos/

target/

# WASM build artifacts (loaded from disk, not bundled)
*.wasm

# Reference repos (cloned locally, not committed)
examples/reference-repos/

# Sealed secrets (generated per-cluster, never commit)
k8s/overlays/*/sealed-secret.yaml

