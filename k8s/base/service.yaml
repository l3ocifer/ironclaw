apiVersion: v1
kind: Service
metadata:
  name: ironclaw
  labels:
    app: ironclaw
spec:
  selector:
    app: ironclaw
  ports:
    - port: 3000
      targetPort: gateway
      protocol: TCP
      name: gateway
