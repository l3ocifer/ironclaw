# Template — do NOT commit with real values.
# Seal with: kubeseal --controller-namespace sealed-secrets --format yaml < secrets-template.yaml > sealed-secret.yaml
#
# After filling in values, create the sealed secret:
#   kubectl create secret generic ironclaw-secrets \
#     --namespace ironclaw \
#     --from-literal=DATABASE_URL='postgres://ironclaw:PASSWORD@external-postgres:5432/ironclaw' \
#     --from-literal=ANTHROPIC_API_KEY='sk-ant-...' \
#     --from-literal=OPENAI_API_KEY='sk-...' \
#     --from-literal=GEMINI_API_KEY='...' \
#     --dry-run=client -o yaml | \
#   kubeseal --controller-namespace sealed-secrets --format yaml > sealed-secret.yaml
#
# Then add sealed-secret.yaml to kustomization.yaml resources list.
apiVersion: v1
kind: Secret
metadata:
  name: ironclaw-secrets
  namespace: ironclaw
type: Opaque
stringData:
  DATABASE_URL: "postgres://ironclaw:CHANGEME@external-postgres:5432/ironclaw"
  ANTHROPIC_API_KEY: ""
  OPENAI_API_KEY: ""
  GEMINI_API_KEY: ""
